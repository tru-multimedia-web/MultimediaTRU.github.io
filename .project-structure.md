# 📁 โครงสร้างโปรเจค MultimediaTRU.github.io

## 🏗️ โครงสร้างไฟล์ทั้งหมด

```
MultimediaTRU.github.io/
├── 📁 server/                    # เซิร์ฟเวอร์และ API
│   └── api-server.js            # Node.js Express API Server
├── 📁 scripts/                  # สคริปต์จัดการเซิร์ฟเวอร์
│   ├── start-servers.sh         # เริ่มเซิร์ฟเวอร์ทั้งสอง
│   └── stop-servers.sh          # ปิดเซิร์ฟเวอร์ทั้งสอง
├── 📁 css/                      # ไฟล์สไตล์ชีต
│   ├── styles.css               # สไตล์หลักของเว็บไซต์
│   └── video-generator.css      # สไตล์เฉพาะ video-generator
├── 📁 js/                       # ไฟล์ JavaScript
│   ├── video-gallery.js         # จัดการแกลเลอรี่วิดีโอ
│   ├── video-generator.js       # เครื่องมือจัดการวิดีโอ
│   └── player.js                # ตัวเล่นวิดีโอ
├── 📁 images/                   # รูปภาพและไฟล์สื่อ
│   ├── cover/                   # รูปภาพปกวิดีโอ
│   ├── *.jpg, *.png            # รูปภาพทั่วไป
│   └── logo_multimedia*.png    # โลโก้
├── 📁 data/                     # ฐานข้อมูล
│   └── videos.json              # ข้อมูลวิดีโอทั้งหมด
├── 📁 docs/                     # เอกสารประกอบ
│   ├── README.md                # เมนูเอกสารหลัก
│   ├── guides/                  # คู่มือการใช้งาน
│   ├── api/                     # เอกสาร API
│   └── troubleshooting/         # แก้ไขปัญหา
├── 📄 index.html                # หน้าแรกของเว็บไซต์
├── 📄 video-generator.html      # เครื่องมือจัดการวิดีโอ
├── 📄 player.html               # หน้าเล่นวิดีโอ
├── 📄 package.json              # Dependencies และ scripts
├── 📄 README.md                 # ไฟล์หลักของโปรเจค
├── 📄 START.md                  # คู่มือเริ่มต้น
└── 📄 .gitignore               # ไฟล์ที่ไม่ต้อง commit
```

## 🎯 หน้าที่ของแต่ละโฟลเดอร์

### 🔧 server/
- **api-server.js**: API Server สำหรับจัดการข้อมูลวิดีโอ
- จัดการการอัพโหลดรูปภาพและบันทึกข้อมูล

### 📜 scripts/
- **start-servers.sh**: เริ่ม Web Server + API Server พร้อมกัน
- **stop-servers.sh**: ปิดเซิร์ฟเวอร์ทั้งสองอย่างปลอดภัย

### 🎨 css/
- **styles.css**: สไตล์หลักของเว็บไซต์
- **video-generator.css**: สไตล์เฉพาะหน้า Admin

### 💻 js/
- **video-gallery.js**: จัดการแสดงแกลเลอรี่วิดีโอ
- **video-generator.js**: เครื่องมือสร้างและแก้ไขวิดีโอ
- **player.js**: ควบคุมการเล่นวิดีโอ

### 🖼️ images/
- **cover/**: รูปภาพปกวิดีโอที่อัพโหลด
- รูปภาพทั่วไปของเว็บไซต์

### 💾 data/
- **videos.json**: ฐานข้อมูลวิดีโอทั้งหมด
- ใช้ร่วมกันระหว่าง gallery, player และ admin

### 📚 docs/
- **guides/**: คู่มือการใช้งาน
- **api/**: เอกสาร API
- **troubleshooting/**: แก้ไขปัญหา

## 🚀 วิธีเริ่มใช้งาน

```bash
# เริ่มเซิร์ฟเวอร์
./scripts/start-servers.sh

# เปิดเว็บไซต์
# http://localhost:8080 (Web Server)
# http://localhost:5001 (API Server)
```

## 📝 ไฟล์สำคัญ

| ไฟล์ | หน้าที่ |
|------|---------|
| `index.html` | หน้าแรก แสดงวิดีโอทั้งหมด |
| `video-generator.html` | เครื่องมือจัดการวิดีโอ |
| `player.html` | หน้าเล่นวิดีโอ |
| `server/api-server.js` | API สำหรับจัดการข้อมูล |
| `data/videos.json` | ฐานข้อมูลวิดีโอ |
| `scripts/start-servers.sh` | เริ่มเซิร์ฟเวอร์ |

## 🔄 การทำงานของระบบ

1. **Web Server** (Port 8080): เสิร์ฟไฟล์ HTML, CSS, JS
2. **API Server** (Port 5001): จัดการข้อมูลและไฟล์
3. **Frontend**: เรียก API เพื่อบันทึก/โหลดข้อมูล
4. **Data**: เก็บใน `data/videos.json` และ `images/cover/`

---

*โครงสร้างโปรเจค ณ วันที่: $(date)*